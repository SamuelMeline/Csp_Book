{% extends 'base.html.twig' %}

{% block title %}
	Csp_Book - Nouvelle panoplie
{% endblock %}

{% block body %}
	<div class="new-items">
		<h1 class="page-title">Création d'une panoplie</h1>

		{{ form_start(form) }}

		{# <div class="form-group-images">
			{{ form_label(form.image) }}
			{{ form_widget(form.image, {'attr': {'class': 'image-input'}}) }}
			<div class="form-error">
				{{ form_errors(form.image) }}
			</div>
		</div> #}

		<div class="form-group">
			{{ form_label(form.name) }}
			{{ form_widget(form.name, {'attr': {'class': 'name-input'}}) }}
			<div class="form-error">
				{{ form_errors(form.name) }}
			</div>
		</div>

		<div class="form-group">
			{{ form_label(form.difficulty) }}
			{{ form_widget(form.difficulty, {'attr': {'class': 'difficulty-input'}}) }}
			<div class="form-error">
				{{ form_errors(form.difficulty) }}
			</div>

		<div class="form-group">
			{{ form_label(form.description) }}
			{{ form_widget(form.description, {'attr': {'class': 'description-input'}}) }}
			<div class="form-error">
				{{ form_errors(form.description) }}
			</div>

		<div class="form-group">
			{{ form_label(form.price) }}
			{{ form_widget(form.price, {'attr': {'class': 'price-input'}}) }}
			<div class="form-error">
				{{ form_errors(form.price) }}
			</div>
		</div>

		<div class="form-group">
			{{ form_label(form.isFavorite) }}
			{{ form_widget(form.isFavorite, {'attr': {'class': 'isFavorite-input'}}) }}
			<div class="form-error">
				{{ form_errors(form.isFavorite) }}
			</div>
		</div>

		<div class="form-group">
			{{ form_label(form.items) }}
			{{ form_widget(form.items, {'attr': {'class': 'items-input'}}) }}
			<div class="form-error">
				{{ form_errors(form.items) }}
			</div>
		</div>

		<div class="form-group">
			{{ form_row(form.submit, {'attr': {'class': 'submit-button'}}) }}
		</div>
	</div>
	{{ form_end(form) }}
	</div>
{% endblock %}
